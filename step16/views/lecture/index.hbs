<!-- HTML에서 JavaScript 분리 -->
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>HTML</title>
  <link rel="stylesheet" href="../node_modules/bootstrap/dist/css/bootstrap.min.css">
  <style>
  .container {
    width: 600px;
    margin: 0px auto;
  }
  a[disabled] {
    pointer-events: none;
  }
  </style>
</head>
<body>
<div class="container">
<h1>강의 목록</h1>
<table id="manager-tbl" class="table table-hover">
<thead>
  <tr>
    <th>강의번호</th>
    <th>제목</th>
    <th>시작일</th>
    <th>종료일</th>
    <th>수강가능인원</th>
    <th>수업료</th>
    <th>총시간</th>
  </tr>
</thead>
<tbody>
  {{#each data}}
    <tr>
      <td>{{lno}}</td>
      <td><a href="detail.do?no={{lno}}">{{titl}}</td>
      <td>{{sdt2}}</td>
      <td>{{edt2}}</td>
      <td>{{qty}}</td>
      <td>{{pric}}</td>
      <td>{{thrs}}</td>
    </tr>
  {{/each}}
</tbody>
</table>

<a href="list.do?pageNo={{prevPageNo}}" class="btn btn-primary" {{disabledPrevBtn}}>이전</a>
<span id="page-no">{{pageNo}}</span>
<a href="list.do?pageNo={{nextPageNo}}" class="btn btn-primary" {{disabledNextBtn}}>다음</a>
<a href="form.do" class="btn btn-primary">추가</a>
</div>


</body>
</html>


<!-- -->
